---
phase: 01-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/sessionguard/session_guard.dart
  - lib/features/sessionguard/session_bootstrap_coordinator.dart
  - lib/services/notifications/notification_service.dart
  - lib/services/notifications/sos_notification_service.dart
  - test/features/sessionguard/session_bootstrap_coordinator_test.dart
  - test/services/notifications/notification_service_routing_test.dart
autonomous: true
requirements:
  - CORE-01
user_setup: []
must_haves:
  truths:
    - "Cold start and logout/login transitions resolve to the correct parent, guardian, or child shell without stale bootstrap side effects."
    - "Notification bootstrap binds once per authenticated uid and SOS taps route through one normalized path."
  artifacts:
    - path: "lib/features/sessionguard/session_guard.dart"
      provides: "Role-based shell selection and role-scoped provider ownership"
      contains: "SessionBootstrapCoordinator("
    - path: "lib/features/sessionguard/session_bootstrap_coordinator.dart"
      provides: "Single authenticated bootstrap and cleanup flow"
      contains: "_runSessionBootstrap"
    - path: "lib/services/notifications/notification_service.dart"
      provides: "Normalized remote-message routing and tap dedupe"
      contains: "_lastHandledTapKey"
    - path: "test/features/sessionguard/session_bootstrap_coordinator_test.dart"
      provides: "Automated bootstrap regression coverage"
      min_lines: 40
  key_links:
    - from: "lib/features/sessionguard/session_guard.dart"
      to: "lib/features/sessionguard/session_bootstrap_coordinator.dart"
      via: "SessionGuard wraps resolved shells in the bootstrap coordinator"
      pattern: "SessionBootstrapCoordinator\\("
    - from: "lib/features/sessionguard/session_bootstrap_coordinator.dart"
      to: "lib/services/notifications/sos_notification_service.dart"
      via: "Authenticated bootstrap initializes SOS notification handling"
      pattern: "SosNotificationService\\.instance\\.init"
---

<objective>
Stabilize authenticated shell selection, bootstrap ownership, and notification routing for app startup.

Purpose: Prevent stale session state, duplicated push listeners, and wrong-shell routing during cold start or logout/login churn.
Output: Updated session/bootstrap code plus focused Flutter tests covering the runtime contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-foundation/01-PATTERNS.md
@lib/features/sessionguard/session_guard.dart
@lib/features/sessionguard/session_bootstrap_coordinator.dart
@lib/services/notifications/notification_service.dart
@lib/services/notifications/sos_notification_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten session bootstrap and logout cleanup ownership</name>
  <files>lib/features/sessionguard/session_guard.dart, lib/features/sessionguard/session_bootstrap_coordinator.dart</files>
  <read_first>lib/features/sessionguard/session_guard.dart, lib/features/sessionguard/session_bootstrap_coordinator.dart, .planning/phases/01-runtime-foundation/01-RESEARCH.md</read_first>
  <action>Keep `SessionGuard` as the only widget that chooses `AppShell(mode: ...)` from `SessionGuardResolvedState`; do not add competing role checks in `main.dart` or unrelated view models. In `SessionBootstrapCoordinator`, keep `_handleSnapshotChange()` as the single auth-transition gate and tighten `_runSessionBootstrap()`, `_runPreLogoutCleanup()`, `_runSessionCleanup()`, and `_resetBootstrapState()` so authenticated side effects only run when `currentSession.user?.uid == uid`, `resolvedSession.hasResolvedIdentity == true`, and `widget.isLoggingOut == false`. Reset `_identityResolvedUid`, `_bootstrappedUid`, `_pushInitedForUid`, `_logoutPreparedForUid`, and any session-scoped storage/runtime state during logout before a new uid can enqueue bootstrap. Preserve `NotificationVM.bindUser`, `AuthRuntimeManager.start/stop`, `userVm.watchMe(uid)`, and timezone sync as session-scoped side effects owned by the coordinator.</action>
  <verify>flutter analyze lib/features/sessionguard</verify>
  <acceptance_criteria>
    - `session_guard.dart` still routes authenticated users from `SessionGuardResolvedState`, not ad-hoc role reads
    - `session_bootstrap_coordinator.dart` contains one bootstrap path centered on `_runSessionBootstrap()` and one cleanup path centered on `_runSessionCleanup()`
    - logout/login transitions reset bootstrap ownership fields before a new uid can re-bind notifications or runtime state
  </acceptance_criteria>
  <done>Session shell routing and authenticated side effects have one owner with explicit cleanup before re-bootstrap.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize notification bootstrap and SOS tap routing</name>
  <files>lib/services/notifications/notification_service.dart, lib/services/notifications/sos_notification_service.dart</files>
  <read_first>lib/services/notifications/notification_service.dart, lib/services/notifications/sos_notification_service.dart, functions/src/services/sosPush.ts</read_first>
  <action>Keep `NotificationService` responsible for remote-message normalization, tap dedupe, and handoff to `SosTapRouter.handleTap`. Keep `SosNotificationService` responsible only for local SOS presentation and local resolve actions. Normalize `data['type']` with `.toLowerCase()` at every client FCM entrypoint (`onMessage`, `onMessageOpenedApp`, `getInitialMessage`, and local payload decode), remove the case-sensitive `type != 'SOS'` branch in `_showFromRemoteMessage()`, and make kill-state/opened-app SOS navigation pass through a single deduped path so one push tap cannot double-navigate. Do not change backend payload producers in this phase; absorb mixed-case payloads on the client.</action>
  <verify>flutter analyze lib/services/notifications</verify>
  <acceptance_criteria>
    - `notification_service.dart` lowercases notification `type` before routing taps or local notifications
    - `sos_notification_service.dart` no longer requires uppercase-only SOS payloads
    - only one client path triggers SOS navigation for opened-app and initial-message cases
  </acceptance_criteria>
  <done>Remote notification routing accepts mixed-case payloads and avoids duplicate SOS navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused bootstrap and notification regression tests</name>
  <files>test/features/sessionguard/session_bootstrap_coordinator_test.dart, test/services/notifications/notification_service_routing_test.dart</files>
  <read_first>test/services/background_tracking_status_policy_test.dart, lib/features/sessionguard/session_bootstrap_coordinator.dart, lib/services/notifications/notification_service.dart</read_first>
  <action>Create `flutter_test` coverage that exercises the exact runtime contract from Tasks 1 and 2. Add a session-bootstrap test that proves the coordinator does not re-run authenticated side effects for the same uid and that logout/login transitions clear the prior session before a new shell is considered ready. Add a notification-routing test that feeds mixed-case `SOS` and `sos` payloads through the client routing entrypoints and proves `SosTapRouter.handleTap` is invoked exactly once per payload. Prefer lightweight fakes, injected adapters, or method-channel stubs over new mocking dependencies.</action>
  <verify>flutter test test/features/sessionguard/session_bootstrap_coordinator_test.dart test/services/notifications/notification_service_routing_test.dart</verify>
  <acceptance_criteria>
    - `test/features/sessionguard/session_bootstrap_coordinator_test.dart` asserts same-uid bootstrap dedupe and logout/login cleanup behavior
    - `test/services/notifications/notification_service_routing_test.dart` asserts mixed-case SOS payload normalization and single-route handling
    - the new tests fail if bootstrap side effects duplicate or if SOS taps double-route
  </acceptance_criteria>
  <done>Phase 1 has direct automated coverage for session bootstrap and notification routing regressions.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Flutter session shell selection vs Firebase-authenticated identity resolution
- Authenticated bootstrap side effects vs logout/login transitions and storage cleanup
- FCM payload ingress vs local notification display and in-app navigation
</trust_boundaries>

<stride_register>
  <threat>
    <threat_id>01-01-T1</threat_id>
    <category>Spoofing</category>
    <component>SessionBootstrapCoordinator / SessionGuard</component>
    <severity>high</severity>
    <scenario>Stale uid or profile data survives logout/login, causing the wrong shell or push bindings to remain active for a new user.</scenario>
    <disposition>mitigate</disposition>
    <mitigation_plan>Gate bootstrap on the live authenticated uid plus resolved identity, reset bootstrap ownership fields during logout, and prove same-uid dedupe plus cross-uid cleanup in Flutter tests.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-01-T2</threat_id>
    <category>Denial of Service</category>
    <component>NotificationService / SosNotificationService</component>
    <severity>medium</severity>
    <scenario>Duplicated FCM open/tap listeners process the same SOS payload twice, causing duplicate navigation or duplicate resolve attempts.</scenario>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep one normalized SOS navigation path, dedupe tap handling by payload key, and cover kill-state/opened-app routing with focused tests.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze lib/features/sessionguard lib/services/notifications`
- [ ] `flutter test test/features/sessionguard/session_bootstrap_coordinator_test.dart test/services/notifications/notification_service_routing_test.dart`
- [ ] Mixed-case SOS payloads and logout/login bootstrap churn are covered by automated tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bootstrap ownership is centralized enough that a new uid cannot inherit stale notification or session side effects
- Notification routing accepts mixed-case SOS payloads and dedupes tap handling
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-01-SUMMARY.md`
</output>
