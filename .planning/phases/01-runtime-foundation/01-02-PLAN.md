---
phase: 01-runtime-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/sessionguard/tracking_warmup_controller.dart
  - lib/viewmodels/location/child_location_view_model.dart
  - lib/background/tracking_background_service.dart
  - lib/background/background_tracking_runtime.dart
  - lib/main.dart
  - android/app/src/main/kotlin/com/example/kid_manager/TrackingForegroundService.kt
  - android/app/src/main/kotlin/com/example/kid_manager/TrackingServiceChannel.kt
  - test/features/sessionguard/tracking_warmup_controller_test.dart
  - test/services/tracking_background_service_test.dart
autonomous: true
requirements:
  - CORE-02
user_setup: []
must_haves:
  truths:
    - "Only one publisher owns child tracking updates at a time across foreground, background, and cold-start transitions."
    - "Foreground startup and Android foreground-service lifetime agree on whether the runtime is actually running."
  artifacts:
    - path: "lib/features/sessionguard/tracking_warmup_controller.dart"
      provides: "Lifecycle-aware tracking ownership orchestration"
      contains: "_syncRoleTracking"
    - path: "lib/viewmodels/location/child_location_view_model.dart"
      provides: "Foreground publisher with explicit service-backed handoff"
      contains: "_publishingHandledByService"
    - path: "lib/background/tracking_background_service.dart"
      provides: "Deduped native service bootstrap and runtime config persistence"
      contains: "startForCurrentUser"
    - path: "test/services/tracking_background_service_test.dart"
      provides: "Automated service handoff coverage"
      min_lines: 40
  key_links:
    - from: "lib/features/sessionguard/tracking_warmup_controller.dart"
      to: "lib/viewmodels/location/child_location_view_model.dart"
      via: "Controller starts, stops, and refreshes self-tracking based on lifecycle snapshots"
      pattern: "childLocationVm\\.(startLocationSharing|refreshLifecycleRouting|stopSharing)"
    - from: "lib/background/tracking_background_service.dart"
      to: "android/app/src/main/kotlin/com/example/kid_manager/TrackingServiceChannel.kt"
      via: "MethodChannel start/stop/isRunning bridge"
      pattern: "MethodChannel\\('tracking_service'\\)"
---

<objective>
Stabilize foreground/background tracking handoff across Flutter and Android service boundaries.

Purpose: Prevent duplicate tracking publishers, stale service ownership, and cold-start service thrash that can corrupt child location state.
Output: Updated runtime handoff code plus focused Flutter tests for lifecycle and service ownership.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-foundation/01-PATTERNS.md
@lib/features/sessionguard/tracking_warmup_controller.dart
@lib/viewmodels/location/child_location_view_model.dart
@lib/background/tracking_background_service.dart
@lib/background/background_tracking_runtime.dart
@lib/main.dart
@android/app/src/main/kotlin/com/example/kid_manager/TrackingForegroundService.kt
@android/app/src/main/kotlin/com/example/kid_manager/TrackingServiceChannel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make lifecycle routing use one explicit tracking owner</name>
  <files>lib/features/sessionguard/tracking_warmup_controller.dart, lib/viewmodels/location/child_location_view_model.dart</files>
  <read_first>lib/features/sessionguard/tracking_warmup_controller.dart, lib/viewmodels/location/child_location_view_model.dart, lib/services/location/background_tracking_status_policy.dart</read_first>
  <action>Keep `TrackingWarmupController` as the lifecycle orchestrator, `ChildLocationViewModel` as the foreground publisher, and `BackgroundTrackingRuntime` as the background publisher. Use `TrackingWarmupSnapshot.selfTrackingKey` plus `_lastAppliedTrackingKey` as the idempotency guard for role/lifecycle transitions. Ensure resumed-app flow always reclaims ownership by stopping the background service before foreground publishing resumes, and ensure background flow only calls `TrackingBackgroundService.startForCurrentUser()` once per effective config when `allowTracking == true`. Preserve the difference between parent `currentOnly` sharing and child/guardian full tracking; do not collapse them into one code path.</action>
  <verify>flutter analyze lib/features/sessionguard lib/viewmodels/location</verify>
  <acceptance_criteria>
    - `tracking_warmup_controller.dart` uses one lifecycle decision path to choose foreground vs service-backed publishing
    - `child_location_view_model.dart` does not start a second publisher when `_publishingHandledByService` is already true
    - resumed-app flow explicitly hands ownership back to the foreground publisher before GPS streaming continues
  </acceptance_criteria>
  <done>Lifecycle changes cannot leave both the foreground publisher and background runtime active at the same time.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten native service bootstrap and cold-start semantics</name>
  <files>lib/background/tracking_background_service.dart, lib/background/background_tracking_runtime.dart, lib/main.dart, android/app/src/main/kotlin/com/example/kid_manager/TrackingForegroundService.kt, android/app/src/main/kotlin/com/example/kid_manager/TrackingServiceChannel.kt</files>
  <read_first>lib/background/tracking_background_service.dart, lib/background/background_tracking_runtime.dart, lib/main.dart, android/app/src/main/kotlin/com/example/kid_manager/TrackingForegroundService.kt, android/app/src/main/kotlin/com/example/kid_manager/TrackingServiceChannel.kt</read_first>
  <action>Preserve `TrackingRuntimeStore` as the source of truth for background runtime config and readiness. Keep `TrackingBackgroundService.startForCurrentUser()` as the only native start path, but make cold-start behavior config-aware: replace the unconditional startup stop in `main()` with logic that only tears down the service when the stored config is stale, disabled, or belongs to a session the foreground app must reclaim. In the native layer, keep `TrackingForegroundService.isRunning` aligned with actual FlutterEngine lifecycle, make failed engine startup clear the running bit, and make STOP clear any lingering runtime-ready state before the next start. Preserve `waitUntilReady()` as the handoff signal from native host to Flutter runtime.</action>
  <verify>flutter analyze lib/background lib/main.dart</verify>
  <acceptance_criteria>
    - `main.dart` no longer stops the tracking service blindly on every foreground startup
    - `tracking_background_service.dart` still dedupes equivalent start requests and persists config before native start
    - Android `isRunning` reflects real engine lifetime and does not remain true after failed startup or stop
  </acceptance_criteria>
  <done>Cold start and Android service lifetime share one config-aware contract for background tracking ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused service-handoff regression tests</name>
  <files>test/features/sessionguard/tracking_warmup_controller_test.dart, test/services/tracking_background_service_test.dart</files>
  <read_first>test/services/background_tracking_status_policy_test.dart, lib/features/sessionguard/tracking_warmup_controller.dart, lib/background/tracking_background_service.dart</read_first>
  <action>Create focused `flutter_test` coverage for the handoff contract from Tasks 1 and 2. Add a controller test that exercises foreground-to-background and background-to-foreground transitions for child or guardian self-tracking, proving the controller does not schedule duplicate starts for the same `selfTrackingKey`. Add a service test that stubs the `tracking_service` method channel and asserts start dedupe, config persistence, and readiness gating using `TrackingBackgroundService.debugStats()` or equivalent test-visible hooks instead of log parsing.</action>
  <verify>flutter test test/features/sessionguard/tracking_warmup_controller_test.dart test/services/tracking_background_service_test.dart test/services/background_tracking_status_policy_test.dart</verify>
  <acceptance_criteria>
    - `test/features/sessionguard/tracking_warmup_controller_test.dart` proves lifecycle resync does not create duplicate publishers
    - `test/services/tracking_background_service_test.dart` proves equivalent start requests dedupe and readiness gating works
    - the new tests fail if ownership flips without stopping the previous publisher first
  </acceptance_criteria>
  <done>Phase 1 has direct automated coverage for the foreground/background tracking handoff contract.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Flutter lifecycle orchestration vs Android foreground-service lifetime
- Foreground publisher vs headless background publisher
- Cold-start startup sequence vs persisted tracking runtime config
</trust_boundaries>

<stride_register>
  <threat>
    <threat_id>01-02-T1</threat_id>
    <category>Denial of Service</category>
    <component>TrackingWarmupController / ChildLocationViewModel</component>
    <severity>high</severity>
    <scenario>Foreground and background publishers both remain active after a lifecycle change, creating duplicate location writes or conflicting tracking status.</scenario>
    <disposition>mitigate</disposition>
    <mitigation_plan>Make lifecycle routing explicitly choose one owner, stop the previous publisher before handoff, and lock the contract down with controller and service tests.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-02-T2</threat_id>
    <category>Tampering</category>
    <component>TrackingRuntimeStore / TrackingForegroundService</component>
    <severity>medium</severity>
    <scenario>Stale runtime config or stale native running state survives a restart, causing the app to trust a service instance that is no longer valid.</scenario>
    <disposition>mitigate</disposition>
    <mitigation_plan>Make startup stop logic config-aware, clear running state on native failures, and verify ready-state transitions through method-channel-backed tests.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze lib/features/sessionguard lib/viewmodels/location lib/background lib/main.dart`
- [ ] `flutter test test/features/sessionguard/tracking_warmup_controller_test.dart test/services/tracking_background_service_test.dart test/services/background_tracking_status_policy_test.dart`
- [ ] Android foreground-service lifetime and Flutter runtime ownership are covered by automated handoff tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Foreground/background transitions leave exactly one tracking publisher active
- Cold-start logic no longer tears down a valid background runtime without checking persisted ownership first
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-02-SUMMARY.md`
</output>
