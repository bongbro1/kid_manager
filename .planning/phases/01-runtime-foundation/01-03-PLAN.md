---
phase: 01-runtime-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .gitignore
  - package.json
  - functions/package.json
  - functions/test/tracking_location_notifications.test.mjs
  - functions/test/sos_initial_fanout.test.mjs
autonomous: true
requirements:
  - CORE-03
user_setup: []
must_haves:
  truths:
    - "Developers can run one local Phase 1 regression entrypoint without a device or emulator."
    - "Session bootstrap, tracking handoff, and backend notification contracts are covered by runnable automated checks."
  artifacts:
    - path: "package.json"
      provides: "Top-level Phase 1 regression scripts"
      contains: "test:phase1"
    - path: "functions/package.json"
      provides: "Functions-specific Phase 1 regression scripts"
      contains: "test:phase1:functions"
    - path: "functions/test/tracking_location_notifications.test.mjs"
      provides: "Backend tracking-notification contract coverage"
      min_lines: 40
  key_links:
    - from: "package.json"
      to: "functions/package.json"
      via: "Top-level regression script delegates to the functions runtime suite"
      pattern: "functions"
    - from: "functions/test/sos_initial_fanout.test.mjs"
      to: "functions/src/services/sosPush.ts"
      via: "Node tests lock down SOS fanout contract"
      pattern: "sendSosPush|SOS"
---

<objective>
Repair the local regression harness for runtime-critical flows and make the new checks easy to run.

Purpose: Protect the behavior from Plans 01-01 and 01-02 with a stable, discoverable command surface instead of stale scripts and ignored test directories.
Output: Fixed ignore rules, exact package scripts, and extended Functions tests for runtime notification contracts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation/01-RESEARCH.md
@.planning/phases/01-runtime-foundation/01-PATTERNS.md
@package.json
@functions/package.json
@functions/test/tracking_location_notifications.test.mjs
@functions/test/sos_initial_fanout.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair test discovery and Phase 1 script entrypoints</name>
  <files>.gitignore, package.json, functions/package.json</files>
  <read_first>.gitignore, package.json, functions/package.json, .planning/phases/01-runtime-foundation/01-RESEARCH.md</read_first>
  <action>Remove ignore rules that hide `test/`, `tests/`, and `**/*_test.dart` while keeping generated build artifacts, `functions/lib/`, and Python cache directories ignored. In root `package.json`, add exact Phase 1 commands named `test:phase1:flutter`, `test:phase1:functions`, and `test:phase1` that point to the runtime suites introduced by Plans 01-01 and 01-02 plus the Functions checks below. In `functions/package.json`, replace the stale missing-file script with `test:phase1:functions` that runs `node --test test/tracking_location_notifications.test.mjs test/sos_initial_fanout.test.mjs`. Keep device/Appium smoke automation out of the default Phase 1 entrypoint so the harness stays runnable on a fresh workstation.</action>
  <verify>npm --prefix functions run test:phase1:functions</verify>
  <acceptance_criteria>
    - `.gitignore` no longer hides new Flutter runtime tests from git status
    - root `package.json` contains `test:phase1:flutter`, `test:phase1:functions`, and `test:phase1`
    - `functions/package.json` points to existing `.mjs` test files instead of missing `.cjs` files
  </acceptance_criteria>
  <done>The repo exposes one discoverable Phase 1 regression entrypoint and no longer hides the tests it depends on.</done>
</task>

<task type="auto">
  <name>Task 2: Extend backend notification contract coverage</name>
  <files>functions/test/tracking_location_notifications.test.mjs, functions/test/sos_initial_fanout.test.mjs</files>
  <read_first>functions/test/tracking_location_notifications.test.mjs, functions/test/sos_initial_fanout.test.mjs, functions/src/services/trackingLocationNotifications.ts, functions/src/services/sosPush.ts</read_first>
  <action>Extend the existing Node tests to pin the backend contracts Phase 1 depends on. In `tracking_location_notifications.test.mjs`, assert recipient filtering for parents vs assigned guardians, event-key/status normalization, and TTL expiry behavior. In `sos_initial_fanout.test.mjs`, assert that SOS fanout targets only adult family managers, excludes the child sender, and preserves the payload contract the client normalizes in Plan 01. If production intentionally emits uppercase `SOS`, keep that expectation explicit in the test so future backend changes cannot silently break the client contract.</action>
  <verify>node --test functions/test/tracking_location_notifications.test.mjs functions/test/sos_initial_fanout.test.mjs</verify>
  <acceptance_criteria>
    - `tracking_location_notifications.test.mjs` asserts the status/event-key and guardian filtering rules used by caregiver alerts
    - `sos_initial_fanout.test.mjs` asserts only adult managers receive SOS fanout and that the child sender is excluded
    - backend tests pin the payload contract that the Phase 1 client logic depends on
  </acceptance_criteria>
  <done>Backend notification behavior for Phase 1 runtime alerts is covered by deterministic Node tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Developer command surface vs actual runnable test files
- Client runtime assumptions vs backend notification payload contracts
- Git ignore rules vs newly added regression coverage
</trust_boundaries>

<stride_register>
  <threat>
    <threat_id>01-03-T1</threat_id>
    <category>Repudiation</category>
    <component>Package scripts / .gitignore</component>
    <severity>high</severity>
    <scenario>Tests exist but are hidden by ignore rules or stale scripts, so developers cannot reliably prove runtime behavior before shipping.</scenario>
    <disposition>mitigate</disposition>
    <mitigation_plan>Expose exact Phase 1 script entrypoints, unhide runtime test directories, and require the aggregated command in verification.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-03-T2</threat_id>
    <category>Tampering</category>
    <component>Functions notification payload contracts</component>
    <severity>medium</severity>
    <scenario>Backend payload shape or recipient filtering changes silently, breaking the client-side routing assumptions added in Plans 01-01 and 01-02.</scenario>
    <disposition>mitigate</disposition>
    <mitigation_plan>Extend the existing Node tests to pin status, recipient, TTL, and SOS payload expectations with direct assertions.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `flutter test test/features/sessionguard/session_bootstrap_coordinator_test.dart test/features/sessionguard/tracking_warmup_controller_test.dart test/services/notifications/notification_service_routing_test.dart test/services/tracking_background_service_test.dart test/services/background_tracking_status_policy_test.dart`
- [ ] `npm --prefix functions run test:phase1:functions`
- [ ] `npm run test:phase1`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 runtime regressions are runnable through stable package-script entrypoints
- Backend notification tests pin the contracts that the client runtime depends on
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation/01-03-SUMMARY.md`
</output>
